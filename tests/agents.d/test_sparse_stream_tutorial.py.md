# test_sparse_stream_tutorial.py

- `tests/test_sparse_stream_tutorial.py`: pins the bounded 4,096×4,096
  procedural sparse world, 32-page LRU protocol, preflight-before-mutation
  boundary, deterministic regeneration and indeterminate retry self-checks,
  narrow Wasm adapter, accessible browser state, fixed responsive embed, and
  required workflow scheduling.
