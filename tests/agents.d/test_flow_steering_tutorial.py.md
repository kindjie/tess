# test_flow_steering_tutorial.py

- `tests/test_flow_steering_tutorial.py`: pins the compiled 32x24
  `DistanceFieldProduct` steering model, exact one-label descent and fixed
  tie-breaking seam, narrow Wasm adapter, fixed responsive tutorial embed,
  accessible controls and textual state, reduced-motion behavior, real-browser
  transition/overflow checks, navigation, and required workflow scheduling.
