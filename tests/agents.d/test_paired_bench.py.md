# test_paired_bench.py

- `tests/test_paired_bench.py`: pins the paired sentinel definitions, source
  map, ABBA orchestration, fail-closed parsing, paired bootstrap, multiple-test
  adjustment, rendering, and suspect-scoped confirmation. Source-map areas
  must equal the performance classifier's sensitive areas, and confirmation
  is capped at 64 benchmark names.
