# test_check_docs_links.py

- `test_check_docs_links.py`: pins generated-site links and anchors plus the
  Pages browser harness. One absolute deadline spans connection, upgrade,
  fragmented reads, and command events, so partial progress cannot extend the
  timeout indefinitely. Doxygen quick-index exceptions remain narrowly scoped.
