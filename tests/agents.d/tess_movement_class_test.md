# tess_movement_class_test

- `tess_movement_class_test`: pins the compile-time movement vocabulary,
  policy compatibility, composed passability and entry costs, and parity of
  the `WalkableField` identity class with the legacy field tag. Cost
  normalization treats zero and negative values as impassable and saturates
  overflow through a widened comparison.
