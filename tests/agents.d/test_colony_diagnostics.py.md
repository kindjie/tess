# test_colony_diagnostics.py

- Pins the example-local authority boundary: the separately compiled
  diagnostics host reuses the colony model, injects lifecycle accounting
  before goal admission, and instruments every target translation unit with
  one consistent compile gate.
- Static assertions complement the native and browser checks. They reject a
  return to synthetic path/queue work, a public Tess flow-health panel, or a
  browser surface that cannot communicate conservation identities and real
  colony work without relying on canvas pixels.
