# tess_delta_frame_lifetime_ndebug_test

- `tess_delta_frame_lifetime_ndebug_test`: recompiles the DeltaFrame lifetime
  contract with `NDEBUG`; the stale-view death tests prove generation checks
  remain active when assertions are disabled.
