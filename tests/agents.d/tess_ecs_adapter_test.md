# tess_ecs_adapter_test

- `tess_ecs_adapter_test`: pins the dependency-free ECS concepts, occupancy
  index, indexed path-agent movement, and plain-array tick pipeline. It checks
  field/index synchronization after every transition and allocation-free warm
  operation after explicit reserve.
