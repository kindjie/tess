# tess_path_view_test

- `tess_path_view_test`: pins `PathResult`'s non-owning `PathView`, including
  storage identity, composable bounds-clamped suffixes, real A* scratch views,
  and allocation-free observation.
