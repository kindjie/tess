# test_measure_traffic_lab.py

- `tests/test_measure_traffic_lab.py`: pins the advisory Traffic Lab campaign
  runner's percentile sample floors, nearest-rank calculation, and fail-closed
  parsing of native fixed-tick sample streams. Timing values remain advisory;
  deterministic scenario and search-budget checks retain correctness authority.
