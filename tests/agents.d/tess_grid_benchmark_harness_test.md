# tess_grid_benchmark_harness_test

- `tess_grid_benchmark_harness_test`: pins strict, bounded Moving AI fixture
  parsing and compile-time-shape loading. Its independent path oracle must
  agree exactly with tess A* in both movement modes; the fixed-point acceptance
  interval is intentionally asymmetric.
