# test_colony_tutorial.py

- `tests/test_colony_tutorial.py`: pins every C++ excerpt to named regions in
  the compiled shared model, the unchanged 128x128 composition,
  compact high-DPI article mode, accessible controls and textual state,
  reduced-motion behavior, real-browser transition/overflow checks,
  navigation, and required workflow scheduling.
