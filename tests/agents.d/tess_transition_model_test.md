# tess_transition_model_test

- `tess_transition_model_test`: pins forward/reverse regular and provider
  transition agreement, ordering, cost scaling, sparse uncertainty, and static
  cost-range classification. Extreme coordinates are rejected before signed
  neighbor arithmetic, and throwing enumeration sinks propagate rather than
  terminate.
