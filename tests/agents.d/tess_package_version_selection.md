# tess_package_version_selection

- `tess_package_version_selection`: configures synthetic packages to pin 0.x
  same-minor selection, 1.x same-major selection, too-old and incompatible
  rejection, and the rule that prereleases allow only unversioned discovery.
