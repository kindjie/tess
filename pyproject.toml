[project]
name = "tess-dev-tools"
version = "0.0.0"
requires-python = ">=3.10"
dependencies = []

[dependency-groups]
dev = [
  "clang-format==22.1.5",
  "pytest==9.1.1",
  "tiktoken==0.13.0",
]

[tool.uv]
package = false
